# 八字排盘 Web 演示版使用指南

## 🚀 快速开始

### 1. 安装依赖并启动服务

```bash
# 安装依赖
npm install

# 编译并启动服务
npm start
```

服务启动后，会显示如下信息：
```
🎋 八字排盘服务已启动！

   本地访问: http://localhost:3000
   网络访问: http://0.0.0.0:3000

API 端点:
   POST /api/bazi - 计算八字排盘
   GET  /api/examples - 获取示例数据
   GET  /api/health - 健康检查
```

### 2. 访问 Web 界面

打开浏览器访问 http://localhost:3000

## 📱 使用 Web 界面

### 界面特色

🎨 **玄学风格设计**：
- 采用中国传统色彩（金色、墨黑、古铜）
- 卡片式排盘布局，清晰直观
- 阴阳太极图案背景
- 水墨风格视觉效果

### 界面功能

1. **输入区域**：
   - 出生年份：如 1990
   - 出生月份：1-12
   - 出生日期：1-31
   - 出生小时：0-23（24小时制）
   - 出生分钟：0-59
   - 出生秒数：可选，0-59
   - 性别：乾命(男) / 坤命(女)

2. **操作按钮**：
   - **起卦排盘**：根据输入计算八字排盘
   - **随机示例**：快速加载示例数据
   - **重新填写**：清空表单和结果

3. **卡片式结果显示**：
   - 📅 基本信息卡片（公历、农历、性别）
   - ⚡ 四柱八字卡片（年月日时柱，含五行十神）
   - ☀ 日主卡片（命主本人）
   - 🔮 地支藏干卡片（藏干与十神）
   - 🌟 大运流转卡片（10步大运）
   - ✨ 命理要素卡片（胎元、命宫等）

### 使用步骤

1. 填写出生信息
2. 选择性别
3. 点击"计算八字"按钮
4. 查看排盘结果

或者：
1. 点击"随机示例"加载示例数据
2. 点击"计算八字"查看结果

## 🔧 API 使用示例

### 使用 curl 测试

```bash
# 计算八字
curl -X POST http://localhost:3000/api/bazi \
  -H "Content-Type: application/json" \
  -d '{
    "year": 1990,
    "month": 5,
    "day": 15,
    "hour": 10,
    "minute": 30,
    "gender": "male"
  }'

# 获取示例数据
curl http://localhost:3000/api/examples

# 健康检查
curl http://localhost:3000/api/health
```

### 使用 JavaScript/Fetch

```javascript
const calculateBazi = async (birthInfo) => {
  const response = await fetch('http://localhost:3000/api/bazi', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(birthInfo)
  });
  
  const result = await response.json();
  return result;
};

// 使用示例
const birthInfo = {
  year: 1990,
  month: 5,
  day: 15,
  hour: 10,
  minute: 30,
  gender: 'male'
};

calculateBazi(birthInfo).then(result => {
  console.log('八字排盘结果：', result.data);
  console.log('格式化输出：', result.formatted);
});
```

### 使用 Python

```python
import requests
import json

def calculate_bazi(birth_info):
    url = 'http://localhost:3000/api/bazi'
    headers = {'Content-Type': 'application/json'}
    
    response = requests.post(url, json=birth_info, headers=headers)
    return response.json()

# 使用示例
birth_info = {
    'year': 1990,
    'month': 5,
    'day': 15,
    'hour': 10,
    'minute': 30,
    'gender': 'male'
}

result = calculate_bazi(birth_info)
print('八字排盘结果：', json.dumps(result['data'], indent=2, ensure_ascii=False))
```

## 📊 排盘结果说明

### 结果包含以下信息：

1. **出生时间**：
   - 公历时间
   - 农历时间

2. **四柱八字**：
   - 年柱：天干 + 地支
   - 月柱：天干 + 地支
   - 日柱：天干 + 地支（日主）
   - 时柱：天干 + 地支

3. **十神关系**：
   - 天干十神
   - 地支藏干十神

4. **日主信息**：
   - 日主天干
   - 五行属性

5. **地支藏干**：
   - 每个地支的藏干
   - 藏干类型（本气/中气/余气）
   - 藏干对应的十神

6. **大运信息**：
   - 起运年龄
   - 运势方向（顺行/逆行）
   - 10步大运（可扩展）

7. **特殊信息**：
   - 胎元
   - 胎息
   - 命宫
   - 身宫
   - 建除

## 🎨 界面特色

- 🌈 渐变紫色主题
- 📱 响应式设计，支持移动端
- ⚡ 实时计算，无需刷新页面
- 🎯 清晰的结果展示
- 💾 JSON 数据可直接复制使用
- 🔄 一键加载示例数据

## 🛠️ 技术特点

### 前端
- 原生 HTML/CSS/JavaScript
- 无需任何框架
- 优雅的渐变设计
- 流畅的交互体验

### 后端
- Node.js + Express
- RESTful API 设计
- CORS 支持
- 完整的错误处理

### 核心
- TypeScript 编写
- 基于 tyme4ts 库
- 准确的农历转换
- 完整的八字计算

## 📝 示例输出

```
═══════════════════════════════════════════════════════════
                    八字排盘                    
═══════════════════════════════════════════════════════════

出生时间（公历）: 1990年5月15日 10:30:0
出生时间（农历）: 1990年4月21日 10时
性别: 男

───────────────────────────────────────────────────────────
                    四柱八字                    
───────────────────────────────────────────────────────────
        年柱          月柱          日柱          时柱
        庚午          辛巳          庚辰          辛巳
        (金)          (金)          (金)          (金)

───────────────────────────────────────────────────────────
                    十神                    
───────────────────────────────────────────────────────────
        比肩          劫财          日主          劫财

───────────────────────────────────────────────────────────
                    日主                    
───────────────────────────────────────────────────────────
日主: 庚金

───────────────────────────────────────────────────────────
                    地支藏干                    
───────────────────────────────────────────────────────────
年支 午: 丁[正官](本气) 己[正印](中气)
月支 巳: 丙[七杀](本气) 庚[比肩](中气) 戊[偏印](余气)
日支 辰: 戊[偏印](本气) 乙[正财](中气) 癸[伤官](余气)
时支 巳: 丙[七杀](本气) 庚[比肩](中气) 戊[偏印](余气)

... (更多信息)
```

## ⚙️ 配置选项

### 修改端口

```bash
PORT=8080 npm start
```

### 后台运行

```bash
nohup npm start > server.log 2>&1 &
```

### 查看日志

```bash
tail -f server.log
```

## 🔒 注意事项

1. **时间格式**：使用公历（阳历）时间，系统会自动转换为农历
2. **时辰问题**：23:00-01:00 属于子时，跨日问题已自动处理
3. **性别影响**：性别会影响大运的顺逆排列

## 🐛 故障排除

### 服务无法启动

1. 检查端口是否被占用：`lsof -i :3000`
2. 检查是否已编译：`npm run build`
3. 查看错误日志：`cat server.log`

### 计算结果异常

1. 检查输入参数是否合法
2. 确认日期是否存在（如 2月30日无效）
3. 检查年份范围是否合理

### 界面无法访问

1. 确认服务已启动：`curl http://localhost:3000/api/health`
2. 检查防火墙设置
3. 尝试使用 127.0.0.1 代替 localhost

## 📚 相关文档

- [项目 README](./README.md)
- [部署指南](./DEPLOYMENT.md)
- [使用指南](./docs/USAGE.md)
- [tyme4ts 文档](https://github.com/6tail/tyme4ts)

## 💡 提示

- 可以保存常用的生辰信息到浏览器本地存储
- JSON 结果可以导出用于其他应用
- API 支持批量计算（多次调用）
- 适合集成到现有系统中

## 🎯 未来改进

- [ ] 添加结果保存功能
- [ ] 支持批量计算
- [ ] 添加更多可视化图表
- [ ] 支持导出 PDF
- [ ] 添加详细的八字解读

## 📞 支持

如有问题或建议，欢迎提交 Issue！
